json
{
  "name": "shopee-unshortener-api",
  "version": "1.0.0",
  "description": "CORS-bypassing proxy for expanding Shopee links",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
